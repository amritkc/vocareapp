/**
 * Configuration utility for controlling data source
 * Set to true to use mock/dummy data instead of API calls
 * Set to false to use real API (Supabase) data
 */
export const dataSourceConfig = {
  useMockData: false // Set to true by default since server is not responding
};